# Build Documentation Directory

The Rocq build writes generated `.glob` files into this directory. Browsable
proof documentation for the frozen source is already provided under
`../../docs/proof/` in the supplement.
