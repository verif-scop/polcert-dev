# Current Documentation

The active paper-facing contract is `STATE_EQ_CLAIM_LEDGER.md`. It binds the
frozen v9 implementation to theorem surfaces, executable routes, artifact
evidence, and explicit nonclaims.

Read proofs from the source worktree at `../work/verified-compilation-v9/`. Its
`make proof-documentation` target generates browsable Rocq documentation from
the exact checked source. Historical definition maps, proof plans, and design
notes are retained under `../archive/docs/` because their paths, line numbers,
or semantic boundaries predate v9.
