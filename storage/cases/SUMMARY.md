# Storage Validation Case Corpus

Generated by `tools/build_case_folders.py` from `EXAMPLE_CATALOG.json` and `MANIFEST.json`.

Each subdirectory contains positive and negative certificates for one transformation.  Public variables are source-level logical variables; target-only storage names are representation variables and are not observable outputs.

| Case | Group | Positives | Negatives | Correctness reason |
|---|---|---:|---:|---|
| `source_no_alias_abstraction` | `precondition` | 3 | 14 | logical source variables have distinct footprints, so storage reasoning over variables is sound |
| `contextual_frame_preservation` | `context` | 3 | 14 | writes stay inside the allowed fragment footprint and protected frame variables keep their values |
| `affine_interchange` | `schedule_only` | 3 | 11 | instances and storage accesses are unchanged; only legal schedule order changes |
| `index_set_splitting` | `schedule_domain` | 3 | 14 | target subdomains disjointly and exactly cover the source domain |
| `ordinary_tiling` | `schedule_domain` | 3 | 11 | tile projection covers source instances and storage accesses are unchanged |
| `scalar_privatization_expansion` | `storage_expansion` | 3 | 16 | each source temporary live range is represented by a fresh per-instance private cell before public use |
| `private_copy_boundary` | `private_boundary` | 3 | 20 | copy-in initializes private live-ins and unique copy-out commits private live-outs to public variables |
| `private_access_local_instantiation` | `private_access` | 3 | 16 | symbolic private accesses instantiate to declared, in-bounds, hidden private cells |
| `layout_remap_padding` | `layout` | 4 | 20 | logical public cells are represented by an injective in-bounds physical layout map |
| `scratchpad_packing` | `scratchpad` | 3 | 19 | copy-in covers local reads and local buffer cells consistently represent a public tile |
| `scratchpad_copy_out` | `scratchpad` | 3 | 15 | local updates are private until every updated public cell is committed exactly once |
| `scalar_promotion` | `promotion` | 3 | 23 | entry load, scalar updates, and exit store implement the same public cell value |
| `array_contraction` | `reuse_folding` | 3 | 21 | non-injective physical reuse is allowed only for non-overlapping logical lifetimes |
| `inter_array_reuse` | `reuse_folding` | 3 | 18 | arrays share a buffer only across disjoint lifetime intervals with compatible storage |
| `array_expansion_versioning` | `versioning` | 3 | 19 | reads select produced versions and final copy-out selects the source-final version |
| `overlapped_tiling` | `overlap_halo` | 3 | 20 | extra computations are private; commit instances exactly cover public live-outs |
| `reduction_privatization` | `reduction` | 3 | 22 | private accumulators cover source contributions and merge under checked algebraic laws |
| `double_buffering` | `versioning` | 3 | 21 | phase projection identifies the current physical buffer and final projection covers public live-outs |
| `storage_view_composition` | `composition` | 3 | 16 | target-mid and mid-source public views agree on intermediate observables and compose |
