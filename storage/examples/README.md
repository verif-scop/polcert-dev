# Storage Examples

`scalar_privatization_expansion.source.c` and `.target.c` are the smallest
handwritten C snippets used in the top-level survey introduction.

`standalone/` contains the C-like snippets copied from:

```text
/polcert-storage-generalization-20260507/experiments/storage-validation-standalone/cases
```

They correspond to the cases summarized in:

```text
storage/evidence/standalone_coverage.md
```

These examples are not all emitted by Pluto/OpenScop.  They are validation
targets: small source/target pairs that make each storage transformation's
public-variable correctness condition concrete.

